/* global describe */
/* global it */
const chai = require('chai');
chai.should();

describe('Suite', () => {
    describe('SomeAttribute', () => {
        it('Always Succeeds', () => {
        });
    });
});
